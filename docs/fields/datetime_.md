# `_datetime.py` — `DateTime`

```python
ts: datetime = s.DateTime(required=True)
```

`datetime.datetime` ↔ ISO-8601 string. Module-name leading underscore
avoids shadowing the stdlib `datetime` module; exported as
`s.DateTime`.
