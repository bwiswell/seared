[project]
name = "seared"
version = "0.2.0"
description = ""
authors = [
    { name = "Ben Wiswell", email = "benjwiswell@gmail.com" },
]
requires-python = ">=3.11,<4.0"
dependencies = [
    "marshmallow>=3.26.1,<4.0",
]

[project.optional-dependencies]
numpy = ["numpy>=2.4.2,<3.0"]

[dependency-groups]
dev = [
    "pytest>=8.0,<9.0",
    "seared[numpy]",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["seared"]
